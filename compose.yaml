name: taskmaster

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      LOG_LEVEL: DEBUG
    volumes:
      - /dev/log:/dev/log
      - ./taskmaster.toml:/app/taskmaster.toml